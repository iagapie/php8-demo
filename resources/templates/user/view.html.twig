{% extends 'base.html.twig' %}

{% block body %}
    <h1>User ID: {{ id }}</h1>
    <p><a href="{{ full_url_for(uri, 'home') }}">Absolute Home URL</a></p>
    <p><a href="{{ url_for('home') }}">Home URL</a></p>
{% endblock %}
